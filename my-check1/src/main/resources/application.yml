spring:
  application:
    name: my-check1

server:
  port: 8085

# 配置dubbo
dubbo:
  registry:
    # 注册中心地址
    address: zookeeper://127.0.0.1
    port: 2181
  reference:
    com.ghy.www.api.IService1.check: false
  application:
    logger: slf4j