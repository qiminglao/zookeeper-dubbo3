# 应用名称
spring:
  application:
    name: my-multiple-versions-consumer

server:
  port: 8085

# 配置dubbo
dubbo:
  registry:
    # 注册中心地址
    address: zookeeper://127.0.0.1
    port: 2181
  application:
    logger: slf4j